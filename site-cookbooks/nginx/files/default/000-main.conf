server {
  listen 80 default_server;
  root /var/www;
  index index.html index.htm index.php /_h5ai/server/php/index.php;
  server_name karuru.info;
  location / {
		try_files $uri $uri/ /index.html;
		autoindex on;

		location ~ .php$ {
			fastcgi_split_path_info ^(.+.php)(/.+)$;
			fastcgi_pass unix:/var/run/php5-fpm.sock;
			fastcgi_index index.php;
			include fastcgi_params;
		}
	}

	location /karuru {
		autoindex on;

		deny 192.168.62.1;
		allow 192.168.62.0/24;
		allow 127.0.0.1;
		deny all;
	}

	location /karuru/pub {
		autoindex on;

		satisfy any;
		allow 192.168.62.0/24;
		allow 127.0.0.1;
		deny all;

		auth_basic "Files";
		auth_basic_user_file "/var/www/.htpasswd";
	}
}

server {
	listen 443 ssl;
	ssl on;

	ssl_certificate "/etc/nginx/ssl/karuru.info.crt";
	ssl_certificate_key "/etc/nginx/ssl/karuru.info.key";

  root /var/www;
  index index.html index.htm index.php;
  server_name karuru.info;
  location / {
		try_files $uri $uri/ /index.html;
		autoindex on;

		location ~ .php$ {
			fastcgi_split_path_info ^(.+.php)(/.+)$;
			fastcgi_pass unix:/var/run/php5-fpm.sock;
			fastcgi_index index.php;
			include fastcgi_params;
		}
	}

	location /karuru {
		autoindex on;

		deny 192.168.62.1;
		allow 192.168.62.0/24;
		allow 127.0.0.1;
		deny all;
	}

	location /karuru/pub {
		autoindex on;

		satisfy any;
		allow 192.168.62.0/24;
		allow 127.0.0.1;
		deny all;

		auth_basic "Files";
		auth_basic_user_file "/var/www/.htpasswd";
	}
}
